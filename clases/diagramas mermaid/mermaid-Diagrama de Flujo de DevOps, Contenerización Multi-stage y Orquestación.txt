%% ============================================================================
%% Asignatura: Desarrollo de Aplicaciones Web (Código: 0423807T)
%% Facilitador: M.Sc. Ing. Gabriel Alexis Ramírez Sánchez
%% Período Académico: Septiembre, 2026
%% San Cristóbal, Estado Táchira, Venezuela
%% ============================================================================
graph TD
    %% Estilos de Paleta T�cnica
    classDef devops fill:#fff,stroke:#003366,stroke-width:2px,color:#003366,stroke-dasharray: 5 5;
    classDef docker fill:#e6f2ff,stroke:#003366,stroke-width:1.5px,color:#003366;
    classDef compose fill:#fff9e6,stroke:#ffb300,stroke-width:1.5px,color:#b37d00;
    classDef database fill:#f2f2f2,stroke:#666,stroke-width:1.5px,color:#333;

    %% 1. Automatizaci�n DevOps (GitHub Actions)
    subgraph DevOps_Pipeline [1. AUTOMATIZACION DEVOPS - CI y CD]
        GitPush[Developer Push to GitHub]
        GitHubActions[GitHub Actions Runner]
        BuildTest[Compilacion y Pruebas Unitarias xUnit]
    end

    %% 2. Contenerizaci�n Eficiente (Docker Multi-stage)
    subgraph Container_Build [2. CONTENERIZACION - Multi-Stage Build]
        StageBuild[Etapa de Compilacion: .NET SDK y Node]
        StageRun[Etapa de Produccion: Runtime Liviano y Nginx]
    end

    %% 3. Orquestaci�n de Servicios (Docker Compose)
    subgraph Docker_Compose [3. ORQUESTACION - Docker Compose Network]
        ComposeOrch[Docker Compose Engine]
        DB[(Contenedor: PostgreSQL)]
        API[Contenedor: ASP.NET Core 10 API]
        SPA[Contenedor: React SPA servida por Nginx]
    end

    %% Relaciones de Flujo
    GitPush -->|Trigger Pipeline| GitHubActions
    GitHubActions -->|Ejecuta| BuildTest
    BuildTest -->|Docker Build en Servidor| StageBuild
    StageBuild -->|Descarta SDK y reduce tamano a menor a 120MB| StageRun
    
    StageRun -->|Despliegue de Imagenes| ComposeOrch
    ComposeOrch -->|Monta Red Interna| API
    ComposeOrch -->|Persistencia Volumetrica| DB
    ComposeOrch -->|Exposicion Puerto 8080| SPA

    API -->|Verifica disponibilidad healthcheck| DB
    SPA -->|Solicita recursos REST| API

    %% Enlace invisible para forzar orden vertical
    DevOps_Pipeline ~~~ Container_Build
    Container_Build ~~~ Docker_Compose

    %% Aplicaci�n de Estilos a los Bloques
    class GitPush,GitHubActions,BuildTest devops;
    class StageBuild,StageRun docker;
    class ComposeOrch,API,SPA compose;
    class DB database;