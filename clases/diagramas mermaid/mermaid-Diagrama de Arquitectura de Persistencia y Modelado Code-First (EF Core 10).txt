%% ============================================================================
%% Asignatura: Desarrollo de Aplicaciones Web (Código: 0423807T)
%% Facilitador: M.Sc. Ing. Gabriel Alexis Ramírez Sánchez
%% Período Académico: Septiembre, 2026
%% San Cristóbal, Estado Táchira, Venezuela
%% ============================================================================
flowchart TD
    %% ========================================================
    %% ESTILOS VISUALES
    %% ========================================================
    classDef core fill:#FFFFFF,stroke:#003366,stroke-width:2px,stroke-dasharray: 4 4,color:#003366;
    classDef infra fill:#F0F7FF,stroke:#003366,stroke-width:1.5px,color:#003366;
    classDef opt fill:#FFFDF5,stroke:#D97706,stroke-width:1.5px,color:#92400E;
    classDef db fill:#F8FAFC,stroke:#334155,stroke-width:2px,color:#1E293B;

    %% ========================================================
    %% CAPA DE DOMINIO
    %% ========================================================
    subgraph Core_Domain["Capa de Dominio - Core.Domain"]
        ProductEntity["Entidad: Product.cs"]
        CategoryEntity["Entidad: Category.cs"]
    end

    %% ========================================================
    %% CAPA DE INFRAESTRUCTURA
    %% ========================================================
    subgraph Infra_Persistence["Capa de Infraestructura - Persistencia"]
        DbContext["ApplicationDbContext.cs"]
        
        subgraph Fluent_API["Mapeo Fluent API"]
            ProdConfig["ProductConfiguration.cs"]
            CatConfig["CategoryConfiguration.cs"]
        end

        subgraph Data_Seeding["Estrategia de Data Seeding"]
            SeedProcess["ModelBuilder HasData"]
        end

        subgraph Performance_Opt["Optimizaci�n de Consultas"]
            NoTracking["AsNoTracking / ReadOnly"]
        end
    end

    %% ========================================================
    %% MOTOR DE BASE DE DATOS
    %% ========================================================
    subgraph Database_Layer["Base de Datos Relacional"]
        Postgres[("PostgreSQL")]
    end

    %% ========================================================
    %% RELACIONES Y FLUJOS T�CNICOS
    %% ========================================================
    ProductEntity -->|"Mapeo y Reglas"| ProdConfig
    CategoryEntity -->|"Mapeo y Relaci�n"| CatConfig
    
    ProdConfig -->|"ApplyConfigurationsFromAssembly"| DbContext
    CatConfig -->|"ApplyConfigurationsFromAssembly"| DbContext
    SeedProcess -->|"Aplica en OnModelCreating"| DbContext

    NoTracking -.->|"Modo lectura desacoplado"| DbContext
    DbContext -->|"Npgsql / DDL & DML"| Postgres

    %% ========================================================
    %% ASIGNACI�N DE CLASES
    %% ========================================================
    class ProductEntity,CategoryEntity core;
    class DbContext,ProdConfig,CatConfig,SeedProcess infra;
    class NoTracking opt;
    class Postgres db;